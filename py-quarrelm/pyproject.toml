[project]
name = "py-quarrelm"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "narwhals>=2.17.0",
    "numpy>=2.4.2",
    "pyarrow>=23.0.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0",
    "polars>=1.40",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
